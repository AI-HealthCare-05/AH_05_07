#!/usr/bin/env bash
set -euo pipefail

readonly CODEX_TRAILER="Co-authored-by: Codex <noreply@openai.com>"

if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
  echo "error: not inside a Git worktree" >&2
  exit 1
fi

git commit "$@" --trailer "$CODEX_TRAILER"
