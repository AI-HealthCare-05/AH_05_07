# SK7 Asset Gateway

Public contract: `GET|HEAD https://assets.hyeol.app/v1/<mount>/<key>`

Initial mount: `sk7` → `sk7-assets-prod`.

Do not add a Gomdory bucket until the read-only inventory identifies a safe
public prefix. Never bind a user/project upload bucket wholesale merely because
it has spare capacity.
