name: Synthetic question review
on:
  pull_request:
    branches: [main]
    paths:
      - 'tools/question-review/**'
      - 'docs/input-question-review.md'
      - 'web/src/components/ModelV2InputFlow.tsx'
      - 'web/src/components/modelV2Steps.ts'
      - 'web/src/components/modelV2Draft.ts'
      - 'web/src/ui/modelV2ResultState.ts'
      - 'web/package.json'
      - 'web/package-lock.json'
      - '.github/workflows/question-review.yml'
  push:
    branches: [main]
    paths:
      - 'tools/question-review/**'
      - 'docs/input-question-review.md'
      - 'web/src/components/ModelV2InputFlow.tsx'
      - 'web/src/components/modelV2Steps.ts'
      - 'web/src/components/modelV2Draft.ts'
      - 'web/src/ui/modelV2ResultState.ts'
      - 'web/package.json'
      - 'web/package-lock.json'
      - '.github/workflows/question-review.yml'
permissions:
  contents: read
jobs:
  synthetic-review:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Restore exact scene evidence source bytes on Windows
        if: runner.os == 'Windows'
        shell: bash
        run: |
          for path in \
            web/src/ui/companionSceneRegistry.ts \
            web/src/ui/companionPresentationProfiles.ts
          do
            git show "HEAD:$path" > "$path"
          done
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
        working-directory: web
      - run: npx playwright install --with-deps chromium
        working-directory: web
      - run: npm run build
        working-directory: web
      - run: node tools/question-review/verify.cjs
